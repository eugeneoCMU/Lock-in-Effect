# Implementation plan — panel findings → fixes, 2026-07-28

Companion to `REVIEW_v18_panel_2026-07-28.md`. Operating instruction from Eugene: **keep the fundamental idea and every result the same; improve the wording of everything beyond that.** So the plan splits hard:

- **Part A — executed this session.** Wording-level fixes. Zero numbers, hedge scopes, gate-pinned spans, or claims changed. Every edit passed a mechanical invariant check (digits / `$math$` / `\commands` / pinned spans / line count all preserved) plus the full 101-gate suite and 398-test suite.
- **Part B — not executed.** Everything that needs a new run, an external verification, or a decision that is Eugene's to make (including two items that collide with standing decisions). Each carries a concrete spec so a future session can execute it under spec-before-run.

## Hard constraints this plan operates under (verified in this worktree before editing)

1. Every body edit mirrors into `paper/v18/revised_paper_v18_long_abstract.tex` by **regeneration** (canonical with line 30 swapped for the archived abstract line); the variant gate requires identical line count, difference on line 30 only.
2. Zero-slack literals, counts re-verified pre-edit: `positive at every` ×6 · `$+3.0$ to $+8.0$` ×7 · `11.06` ×4 · `76.3\%` ×8 · `$+3.5$ to $+13.1$` ×6 · `$+3.9$ to $+13.1$` ×0 · `60.2\%` ×10 · `13.6\%` ×11.
3. Gate #98 is paragraph-scoped (line 446 is one paragraph and must stay one); gates #99/#100 assert ORDER (abstract range-before-point; §IV cross-design-before-verdict); gate #101 reads the response letter and recomputes the stated 328-word abstract count. 316 gate-pinned manuscript spans were extracted to a checklist and every edit was screened against them.
4. One paragraph per source line — no line splits or merges anywhere.
5. Abstract edits must be word-count-neutral (328) or the unsent response letter must be updated in the same commit; this session chose word-count-neutral.

---

## Part A — executed (wording only, results frozen)

### A1. Panel-flagged wording defects (verified findings #6 and the DA's abstract mislabel)
- **Abstract, line 30**: "fitted to Freddie Mac data" → "run on Freddie Mac data". Path B is literature-calibrated and simulated on Freddie loans; "fitted" overstated estimation. Word-count-neutral (328 preserved). [DA m1]
- **Conclusion, line 860**: "The Federal Reserve's inability to achieve its Quantitative Tightening objectives was not solely a product of…" → "The shortfall of the Federal Reserve's mortgage runoff beneath its Quantitative Tightening caps was not solely a product of…". Removes the 'inability/objectives' frame the paper's own cap-as-ceiling concession contradicts. [EIC W4 / R2 W1 / R3 W4 / DA, verification-corrected scope]

### A2. Manuscript-wide prose pass (panel writing score 63.5 — lowest dimension)
Nine section agents proposed minimal old→new wording edits under a frozen-content protocol (no digits, math, citations, refs, pinned spans, or hedge scopes touched; no line-count changes; no strengthening of any claim). Each proposal was mechanically validated and human-reviewed before applying; the applied set is recorded in this file's execution log below and in the session diff.
**Explicit coverage note:** the pass targeted main text (§§I–VIII). Appendices were left untouched (ledger/spec prose, lowest reader traffic, highest pin density). A follow-up session can extend the same protocol to appendices.

### A3. Verification protocol (run after each batch and at the end)
1. Regenerate the long-abstract variant from the edited canonical.
2. `python3 tools/liveness_gates.py` → must print ALL GATES PASS (101 gates).
3. Zero-slack literal recount → must match the table above exactly.
4. `python3 -m pytest tests/` → 398 pass.
5. `tectonic` build (figures + `.env` symlinked in) → 0 undefined references.

### Execution log
- Batch 1 (A1, two edits): applied; variant regenerated; **ALL GATES PASS**; abstract 328 words. ✔
- Batch 2 (A2): 80 proposals from 9 section agents; 1 rejected on review (ambiguous-pronoun swap at L102, no clear improvement), **79 applied** after per-edit adjudication plus mechanical validation (unique match; digit/math/`\command` token sequence identical; 316 gate-pinned spans intact — prose spans exact-count, single-token identifiers no-decrease; zero-slack literals exact; line count 1269 preserved; abstract untouched). One edit (the calendar-rotation placebo parse fix at L821) was factually verified against the paragraph's own parallel "clears the rule" constructions before acceptance. ✔
- Final verification: variant identical except line 30 ✔ · **101 gates ALL PASS** ✔ · all 8 zero-slack literal counts exact ✔ · **398 tests pass** ✔ · tectonic build **0 undefined references** ✔ · abstract **328 words** ✔.
- Page count moved 116 → **117** (pure reflow from sentence splits; nothing tracks the page count — checked the response letter and all gates). All edits are wording-only: not one digit, math expression, citation, reference, hedge span, or gate-pinned literal changed.

## Part B execution update (second pass, same day, on Eugene's "do your sequence")

Four Part-B items were executed under spec-before-run; final state **102 gates ALL PASS, 410 tests, 119pp, 0 undefined refs**, commits `ca18fcc..27a3e5e`:

- **B2 (interval inference) — run executed, verdict MOVES.** `hazard/floor_inference_correction.py` (spec committed before run): CR1/CR2/CR3 t(30) + Rademacher wild-cluster bootstrap-t on the R1–R4 floor reads, mapped through the committed PCHIP. All parity gates PASS (percentile layer reproduced bit-exactly). Primary R2 wild-t interval: **[+2.80, +8.72]pp** vs committed percentile [+2.97, +8.02] — upper edge +0.70pp exceeds the ex-ante 0.5pp tolerance → **MOVES**: per the pre-committed rule, no manuscript number was restated; **whether and how to restate the binding layer is Eugene's call against artifact `floor_inference_correction_results.json`** (secondaries agree: CR2 [+2.97,+8.55], CR3 [+2.77,+8.77]; lower edge stays above zero, falls below +3.0; max cluster leverage 0.33). The three-site qualifier labels landed regardless (abstract, tab:headline, conclusion), as the rule pre-authorized.
- **B4 (hull in abstract) — executed.** One appended clause; the pinned interval sentence survives verbatim; hull literal count 6→7; abstract 328→352 words with the letter §7 recount updated in the same commit; two new ABSTRACT_POSTURE pins (gate #99 now 6/6) with batteries extended (401 tests at that commit).
- **B6 (elasticity curve) — executed.** `band_low_extension.py`: 20 fresh paired-leg runs, parity P1–P5 PASS, monotone at both floors. Landed as `tab:lowband` + paragraph; at half the adopted central the marginal is +5.1pp (in-sample floor) / +3.5pp (headline floor). Pinned by gate #102.
- **B6b (moving-share bracket) — executed.** `moving_share_bracket.py`: mixture hazard at the committed patch seam, identity probes 3.5e-18/0.0, bracket ordering PASS. Landed at the Path B mapping disclosure: +$37.7B (+4.9pp) at s=0.5, +$19.2B (+2.5pp) at s=0.25, near-proportional in s; `s` labeled a bracketing parameter. Pinned by gate #102.

Still open and unchanged: **B1** (restructure — Eugene), **B3** (Danish buyback pricing or conclusion reword — Eugene), **B2's manuscript restatement** (Eugene, against the MOVES artifact), **B7–B9** as written.

## Part B execution update (third pass, same day, on Eugene's "full restatement + full buyback run")

- **B2 restatement — EXECUTED.** The wild-cluster bootstrap-t interval **[+2.8, +8.7]pp** is now the binding layer at all seven headline sites; the percentile [+3.0, +8.0] survives ×5 as a labeled, demoted mention ("under-covers"). The posture was re-derived, not find-replaced: +5.57 sits just *below* the corrected midpoint (5.76), so "upper-middle member rather than its centre" became "a middle member of an interval whose measured corrections concentrate below it" (gate #98 span updated; the commitment's surviving content — corrections concentrate below the point — is what the new span pins). Width ratio recomputed (0.39×), the now-false "neither correction is reported here" tablenote rewritten, abstract 352→359 words with letter recount, gates #98/#99 + letter literals + three batteries updated. Commit `b0ecd3e`.
- **B3 — EXECUTED as a no-engine pre-committed bracket.** `buyback_credit_bracket.py` (spec `198a32d`, artifact `fca605f`): committed artifacts only — gap_face = +$61.19B (balance-adjustment identity); gap_cash(D) = 61.19 − D×$470.65B early face over the manuscript's committed 32–38% proxy grid → **[−$117.7B, −$89.4B], negative at every grid point → REVERSES**. Landed per the ex-ante rule at five sites (pathb pricing paragraph, tab:headline Danish row, intro, conclusion qualification, trilemma — the signed "modestly improved (3)" is gone; the dissolution claim is scoped to face accounting). Letter §7 gains the development (four items now move against the paper). Gate #103 with a reversion battery case. Commit `fbfb00d`.

**Final state: 103 gates ALL PASS, 422 tests, 119pp, 0 undefined refs.** Remaining open: B1 (restructure), B7 (verdict table — ride inside B1), B8 (null qualifier — recommend leave), B9 (bib repairs).

---

## Part B — not executed: run-gated or Eugene's call

Ordered by the panel's priority; each item restates the roadmap fix with the adversarial-verification correction applied. "Spec" = what a future session would commit before running. **None of these should be run mid-review-session; all obey spec-before-run.**

### B1. [Critical, panel-unanimous] Restructure to ~50–55pp, ≤150-word abstract, apparatus to online appendix
**Collides with a standing decision** — Eugene abandoned page compression 2026-07-26 (HANDOFF_round25 §2.2) and the concision-round record shows ~40% of that round's rewrites strengthened claims past evidence. The panel's independent unanimity (EIC Critical at confidence 5) is new information for that decision, not an override of it. If reopened: the round-25 recommendation (split a methods paper carrying the gate/parity apparatus) is the shape the EIC's "online appendix" remedy points at. **Decision: Eugene.**

### B2. [Critical] Correct the `[+3.0,+8.0]` interval's inferential status — CONFIRMED finding
**Runs:** wild-cluster bootstrap-t (Rademacher weights) and/or CR3/Satterthwaite intervals on the 31-cluster floor reads; propagate replicates through the committed floor-to-marginal grid; report whether the corrected lower edge stays above zero and above +3.0.
**Spec:** grid = the committed clean-band reads; interpretive threshold = "corrected interval contains the percentile interval" vs "widens materially (>0.5pp either edge)"; parity gate = percentile interval reproduces `[+3.0,+8.0]` bit-exactly before the correction runs.
**Wording-only fallback (no run):** the one-tablenote under-coverage concession could be repeated at the interval's other quotation sites — but that adds ~9 hedge repetitions and interacts with gate #99's spans, so it was deliberately NOT done in Part A; it changes the strength-profile of the paper's headline statement, which is a posture call, not a wording call.

### B3. [Critical] Danish signed claim vs. the buyback concession — verification-corrected scope
The abstract is already clean; the defect is the conclusion's signed "≈$61 billion" sentence outrunning the "not established without par accounting" concession two paragraphs earlier.
**Either** price the market-value credit under both incidence brackets (run: repricing overlay on the committed Danish leg artifacts; spec-before-run) **or** reword the conclusion's sentence to state the magnitude as par-accounting-conditional in the sentence itself, not two paragraphs later. The reword variant weakens a stated claim — allowed under "results frozen" only if Eugene agrees the current sentence overstates; **decision: Eugene.**

### B4. [Critical] Abstract statement of form-conditionality — verification-corrected scope
The hull `$+3.5$ to $+13.1$` appears in both summary tables and §V.E but not the abstract. Adding it to the abstract costs ~10 words (breaks 328 neutrality → letter update required in the same commit; gate #101), and interacts with gate #99's posture spans. Mechanical to execute, but it changes what the abstract commits to — **decision: Eugene.** (The panel offered the alternative of affirmatively defending the max form at the off-window anchors; the defense exists at L745 — a pointer from the abstract is the cheapest compliant variant.)

### B5. [Major] Joint Ginnie-overlay × age-standardized-floor cell — the one genuinely never-run cell
**Run:** overlay harness at the 5.51% age-standardized floor read (both harnesses exist per the manuscript). **Spec:** pre-commit the expectation from the informal composition (~+3.0) and the interaction convention (±1pp materiality, matching `concave_additive_marginal`). Alternative remedy (drop +5.6 from the abstract) is a posture change — Eugene's.

### B6. [Major] Elasticity discipline: band extension below 5.5% toward 0; moving-share bracketing
**Runs:** (i) extend the committed band sweep downward so the marginal is a curve in the elasticity with the in-sample non-rejection displayed on it; (ii) bracketing variant applying the imported elasticity to an assumed moving share of the total hazard. Verification confirmed the piecewise-linear transform test and P_q sensitivity already exist — do not re-propose those.

### B7. [Major] Pre-committed vs post-hoc verdict table (DA M2)
No run needed — an audit table assembled from existing artifacts (gate, committed spec, outcome, post-hoc reinterpretation and its direction). New exhibit + text, so it grows the paper against B1; sequence it with the B1 decision.

### B8. [Major] Mechanical-null estimand precision (DA M3)
Wording-adjacent but posture-bearing: qualifying the abstract's "however households react to rates" span would touch a gate-pinned hedge's neighborhood and weaken a committed claim. The matched-depth counterpart (+$63.5B) already appears at L755. **Decision: Eugene** on whether the abstract's null sentence acquires a floor-content qualifier.

### B9. [Minor] Bibliography repairs
(i) `liebersohn2024` key carries year 2025 — key rename is rendering-neutral churn across ~20 cite sites; harmless to skip, trivial to do. (ii) `berger2026` is a load-bearing unpublished SSRN draft — verify against any published version (external check). (iii) Four suggested missing anchors (Du–Forbes–Luzzetti 2024; Agarwal–Driscoll–Laibson 2013; two others in the R2 report) — additions are content, not wording; batch with the next substantive round.

### B10. Explicitly NOT adopted from the roadmap
- Wholesale retirement of "pins"/"identifies" vocabulary: the fifth qualification defines the terms precisely, several instances are gate-pinned, and the substitution is a posture change beyond a wording pass.
- Reordering the abstract to lead with dollars: gate #99 pins the current order; panel remedy conflicts with a committed gate — Eugene's call with a gate change in the same commit if taken.
