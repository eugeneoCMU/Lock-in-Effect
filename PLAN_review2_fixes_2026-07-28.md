# Implementation plan — second-panel findings → work packages (2026-07-28, FOR EUGENE'S REVIEW — nothing executed)

**Source:** `REVIEW2_v18_panel_2026-07-28.md` (fresh panel, 76.3/100, Major Revision; roadmap items R1–R8, S1–S12, P3). Every item below traces to a verified finding. This session touched no manuscript, gate, test, or letter line — this file is a proposal.

**You said the entire results section may be reworked.** WP-E is that rework, sketched as an option with consequences. Everything else is independent of whether you take it.

## 0. Governing constraints (verified against repo history; they bind every package)

1. **Spec-before-run, landing rule before execution.** Every run-class item gets a committed spec *and* a pre-committed manuscript disposition per outcome branch (the B5 protocol — TECHNICAL §43 — is the template; it is what turned B5 from an adjudication liability into displayed rigor). No exceptions, including "cheap" reruns.
2. **Zero-slack literals must be RE-DERIVED, not reused.** HANDOFF_round27 §3's counts predate the §44 abstract cuts (367→280→226) and the option-2 re-pin. Before any edit near a headline literal: grep the current count *including and-forms* (`$+2.8$ and $+8.7$` is not a counted literal — that blindness let a defect survive round 26), read the gate SOURCE (all counts are `>=`, none exact — round 27 lesson: an edit priced as tex+gates+tests+letter can be tex-only), and recount after every batch.
3. **Near pinned spans: tex+gates+tests+letter in ONE commit.** Gate #98 (assembly spans), #99 (abstract posture spans, re-anchored twice since), #100 (§IV order), #101 (letter recount — current claim 226 words), #102 (elasticity exhibits), #104 (verdicts battery). The letter mutation tests hard-code historical counts — check `tests/` for the wrong-count list before changing any counted quantity.
4. **Results-frozen vs posture.** Wording/structural packages change zero numbers and zero hedge scopes. Anything that changes what the paper *claims* (headline anchor, posture sentence, form-conditional policy statement, contribution list) is **posture = your call**, marked ⚖ below.
5. **Agents never near a run** (standing repo rule). Runs are executed in-session by the coordinator under committed specs, or by you.
6. **Do not resurrect the K1 kill.** The "outcome holdout" idea is circular (PLAN_score_improvements K1); R1's "no outcome-holdout months" observation is answered by the existing disclosure, not by a new construction.

---

## WP-A — Statement-layer repairs (restatement only; no numbers move) — HIGHEST VALUE PER COST

Discharges DA-C1, DA-C3's label, DA-M3, M8's abstract residual, frame retirements, and the minor-tier hygiene. All wording-class except two ⚖ items.

- **A1. The four universals** (tex 30, 43, 106, 601): replace "however households react to rates" / "regardless of/under any household response" with the supportable conditional — e.g. "however the *rate-responsive margin* of household behavior moves" or "under any household response to rates *beyond the baseline turnover the floor already embeds*." The abstract already carries the parenthetical concession; the fix makes the main clause consistent with it. Four sentences. Collision: tex 30 is inside gate #99's abstract spans.
- **A2. ⚖ Floor rename** ("involuntary-turnover floor" → "baseline turnover floor"). Raised independently by DA, R2, EIC. High-value but *wide*: the term appears throughout tex, gates, tests, letter, and figure captions. Recommend a mechanical global rename with "involuntary" reserved for the buyout channel (tex 263) — but it is a large diff on a frozen manuscript, so it is your call whether to take it now or note it for the venue revision. A cheaper interim: keep the name, add one definitional sentence at first use stating the label is a modeling reading (the tex 249 concession promoted to the definition site).
- **A3. Interval referent** (DA-C3): at the abstract ("pins" → "bounds the floor-read layer at"), tab:headline row, tab:uncertainty, and §VIII "measured contribution": state in one clause that [+2.8,+8.7] is the floor-read sampling layer at the central elasticity, and quote the δ-curve (tab:lowband) beside it wherever the interval carries the headline. Zero-slack literal adjacency (×N binding interval) — re-derive counts first.
- **A4. Na/independence wording** (DA-M3): drop "independently" for the Fed-projection corroboration (the untestable-embedding concession forbids it); replace "by construction the null contains exactly these two components" with the accurate "scheduled amortization plus a calibrated floor read from realized turnover."
- **A5. Frame retirements** (EIC-W2/W4, DA-M6/M7): §IV title → "Agent-Based Model Results: A Synthetic-Population Stress Test" (or similar — "Falsification" retired); fig:waterfall caption de-named; contribution list 5→3 with #4 reclassified under limitations and #5 as a robustness result; the Introduction's trilemma sentence (tex 51) gains the form/incidence conditionality or the frame is dropped (⚖ — dropping is my recommendation and the EIC's, but it is a framing decision). Gate #100 asserts §IV order — title edit is safe, but screen.
- **A6. Abstract policy sentence** (DA-M8): append the form condition to the final sentence — "…is small, on an accounting that counts repaid principal at face value, and under the production floor form; the additive form roughly doubles the margin." ⚖ (abstract posture span, gate #99, letter recount 226 moves).
- **A7. Hygiene batch** (one commit): tab:bases sign-definition fix; delete stale "I do not sign the direction" (tex 253 — keep tex 255's signed statement); Path A label harmonization to "does not corroborate" (tab:specbox, tab:pathadiag captions); Graybill/Fonseca symmetric directional statement; P_q relabel (verify against App I derivation first — if the value is annual, say annual); remittance-kernel sentence rewritten as deterministic lag with the (0.10/0.60/0.30) profile relabeled as the *empirical settlement smearing*, not the remittance cycle; `liebersohn2024` year fix; unrefereed-2026-source flags at first use (berger2026 version-pin, lesniewski2026, graybill2026); assumability take-up sentence; ESS qualifier (1,332/10,000) at tab:headline and §I's 76.3%; delete/invert the "No single cluster dominates" sentence (or replace with the honest version: "one cluster carries leverage 0.33; the CSS effective cluster count is ~9, which is why the wild-t correction is applied and the percentile read demoted").

**Effort:** one to two sessions. **Risk:** low (all wording), except the two ⚖ items.

## WP-B — Symmetric assembly + posture re-derivation (DA-C2; R2-W3) — THE DECISION-CRITICAL PACKAGE

- **B1. Rebuild tab:assembly** over *all* measured corrections regardless of class: add rows for the moving-share bracket (currently in-sample — see B2), the sub-band δ curve's off-window member (+3.47 at δ=3.25, already committed in tab:lowband), and the concave transform alone (+5.06 off-window, already committed at tex 253); keep the upward counterweights (additive +11.2, Fonseca +11.5). Delete or correct the "both run the other way" sentence. Add tab:verdicts rows for moving_share_bracket and the sub-band extension (they currently have none — verified).
- **B2. Rerun `moving_share_bracket` at the headline off-window floor** (run-class, existing harness, cheap). Spec + landing rule first: pre-commit the proportional-scaling expectation (~+3.0pp at s=0.5, ~+1.5 at s=0.25 by the in-sample proportionality) and the disposition under each branch — including the branch where s=0.5 lands at or below the binding interval's lower edge, which is the outcome that forces B3.
- **B3. ⚖ Re-derive the posture.** With the symmetric list, "+5.6 is a middle member of an interval whose measured corrections concentrate below it" is not derivable as written. Options, in my order of preference: (i) retire the posture sentence entirely and let the range + assembly carry (smallest claim, panel-proof); (ii) restate as "the corrections concentrate in the interval's lower half; the two form-side counterweights sit above it; the range, not any member, is the finding" (accurate under the symmetric list); (iii) defend s≈1 explicitly (rate/term refi incentive is shut at −380bp, so the voluntary hazard at these gaps is nearly all moving — an argument the paper can make from its own rate-gap distribution, no purpose field needed) and keep +5.6 with the defense stated. **This is the single most consequential decision in the plan** — it moves the abstract, §V, §VIII, the letter, and gates #98/#99.

**Effort:** one session after the B2 run. **Blocks:** B3 needs your call before the tex moves.

## WP-C — Inference-layer repairs (R1-W1/W2/W4; DA-C3's substance)

All run-class against existing machinery; each needs a committed spec + landing rule.

- **C1. Floor-read inference rerun**: Webb six-point weights; Bell–McCaffrey/Imbens–Kolesár data-driven df for CR2/CR3; report G* (CSS effective clusters) for the 2018 leg; state B, bootstrap DGP (null-imposed or not), and the studentization scale (floor scale vs post-PCHIP). Landing rule: if the Webb/df-corrected interval widens beyond [+2.8,+8.7], the wider interval becomes binding everywhere the current one is quoted (zero-slack recount); if it does not widen materially, the tablenote gains the diagnostics and nothing else moves.
- **C2. Layer combination**: convolve the floor-read and loan-sample replicate distributions (both replicate sets exist per the manuscript) — or present the headline as a partial-identification interval (union over calibration cells, expanded by sampling error at the extremal cells, Imbens–Manski critical value). ⚖ on presentation form: the IM interval is the principled object and R1 explicitly invites it; it will be *wider* than [+2.8,+8.7]. My recommendation: run the convolution, report it as a labeled second line, and let the venue referee choose — do not silently replace the binding layer.
- **C3. Baseline-hazard layer**: PSA level sweep (75/100/125/150) on the paired legs. Cheap parameter change; the committed regime-split run already brackets the break dimension. Landing rule pre-commits how the layer ranking sentence changes if the PSA range exceeds the floor-read interval (R1's expectation: it will — the ranking sentence then says "the widest disclosed layer is the baseline level; the floor read is the widest layer with a coverage property").
- **C4. In-sample cluster bootstrap** (`bootstrap_pathb_cluster` at the 4.0% floor): discharges the seven-site [+9.17,+9.23] problem with one run. If the 37× factor carries, the in-sample sampling width is ~2pp and the "negligible at reporting precision" sentence is struck at all sites; if it does not, the sites gain "verified under the cluster scheme." Either branch is clean — this is the best effort-to-rigor ratio in the plan.
- **C5. Covariate block at the headline floor** (rerun of the β_F=β_L=0 and Path-A-style-coefficients variants at 4.991%): discharges DA-M1's sharpest instance (the largest disclosed sensitivity never run where the headline lives).
- **C6. Path A repairs** (R1): recompute the ridge-drop check under spec v4; resimulate the propagated interval under full replicate coefficient vectors (198 vectors exist); 50 dispersed cold starts on the production panel. Landing rule: R1's own expectation is the exclusion verdict survives and possibly strengthens — pre-commit that outcome's wording and the surprise branch.

**Effort:** C1–C5 are each sub-day runs on existing harnesses; C6 is a half-day. Spec-writing is the real cost.

## WP-D — Mixture-form curve (R1-W3; resolves the form-dependence honestly)

Run the floor as a mixture: share *s* of the measured floor enters additively (competing involuntary cause), 1−s as a floor on total turnover; report the marginal as a curve in s ∈ [0,1] with endpoints reproducing the max form (s→0 side… verify orientation in spec) and the additive form. Machinery: structurally identical to `moving_share_bracket` per R1. **⚖ Landing rule is a posture decision**: the paper's own "well under half" semantics puts the honest headline point on the curve's interior — which moves the headline number. Alternative (no run): state the form-conditional pair at every policy-conclusion site including the abstract (A6 covers the abstract) and present the existing two forms as the curve's endpoints. My recommendation: run the curve (it is the referee-proof object), but decide the headline disposition at spec time, not after.

## WP-E — ⚖ Results-section rework (your authorization; EIC-W3/W5, R1-D5, R8)

The panel-consistent reorganization, as one option:

1. **§III (benchmark) absorbs**: the tab:headline caption's 51.0% derivation; the zero-months diagnosis (WP-H1); the expectations-conditioning statement.
2. **§IV (ABM) compresses to ~2–3pp**: stress-test framing (A5), cross-design result stated with the SMD infeasibility as the clean falsification, specification to the replication package. (Prior plan item #31 already pointed here.)
3. **§V (hazard/results core) gains the offstage load-bearing exhibits**: tab:oosfloor, tab:floorband, tab:lowband move in from App. J; tab:params moves in from App. D; the inference ladder becomes a small table (percentile / CR2 / CR3 / wild-t / G* / leverage) replacing the 30-line parenthetical; tab:headline's marginal cell splits into "operative interval" + "sensitivity catalogue."
4. **§VII (policy)**: cap-design arithmetic promoted to a named subsection directly after the benchmark complement, with nyfed2022 credited for the qualitative anticipation and the ratio claimed as the quantification (the arbitrated split); the state-contingent cap restated as a band; the WAL table moves up beside it with a normal-turnover row (R3-W3).
5. **Danish material**: either tightened in place under WP-F, or — the EIC's and my structural recommendation — **split to a companion paper** (rule-only counterfactual + incidence bracket + TBA/covered-bond comparison), leaving a two-paragraph summary. ⚖ This is the same length decision you closed on 2026-07-26; the new panel re-raises it independently (EIC: "will not send to referees at 125 pages"). I flag it once, per the standing rule, and stop.

**Consequences to price**: every move renumbers lines (land line-anchored WPs first); the two-PDF split point (p.82/83 → now p.79/80-ish) recomputes; gate spans re-anchor; the long-abstract variant regenerates.

## WP-F — Danish package (R3-W1/W2/W4/W5; R2-W5d; S1/S9/S10)

- **F1. Fine-grid production-anchor sweep, 0–3% refinance-in-place CPR** (run-class, existing gap-sweep harness): report the gap band, not the 0% point, as the Danish headline row. Landing rule: pre-commit the band presentation.
- **F2. External validation — EXECUTED 2026-07-28 (Eugene-authorized), VERDICT RULE-A′**: deep-discount (≤2% coupon) Danish callable bonds redeemed at 26.4%/yr over 2022M07–2023M12 (stock −1,202→747bn DKK; extraordinary ≥22.4%/yr after the pre-committed allowance) — the ≈0 pin is contradicted by realized data at >2× the committed threshold. Spec + artifacts: SPEC_danish_redemption_validation_2026-07-28.md, hazard/data/danish_external_validation/ (commits 54b8992, 7959969, 9cd4e91, 9b1ec8c). CONSEQUENCE per the pre-committed dispositions: F1's band presentation is now MANDATORY (not optional), the band anchored by a realized-implied contribution, and §V.C states the pin's contradiction. Original proposal text follows for the record: realized Danish redemption/conversion speeds 2022–2025 vs the leg's 5.61% mean CPR. If realized speeds are materially above 5.61%, the ≈0 pin is falsified by data and F1's band is mandatory; if not, the pin gains its first external support. Needs your OK for the external data pull; ingestion is a comparison table, not a model input.
- **F3. Restatements** (wording): the price→quantity inference stated and defended or replaced; "Danish structure" → "Danish payoff rule" globally with one paragraph enumerating what the transplant does not import and each omission's sign (product mix signs *against* via scheduled amortization — R3's point, and it strengthens the paper's own null logic); the TBA paragraph rewritten around series-level vs deliverable-pool homogeneity with berg2018's comparative content and Denmark's own liquid market engaged; "uncompensated" dropped or supported; U.S. tax footnote (COD income at ordinary rates, IRC §61(a)(11); post-TCJA MID reality) — both corrections strengthen the "negligible" conclusion.

## WP-G — Coupon-convention reconciliation (R2-W1; S2)

Sequence matters: **G1 (internal check, no edit)** — determine from the SOMA CUSIP parse what the "coupon" field actually is (security pass-through vs note rate) and what rate the empirical CPR back-out amortizes at; reconcile the 3.9% sample WAC to its bucket shares (weighting definition). **G2** — if the wedge is real: label every coupon column, convert with vintage g-fee + 25bp servicing (public FHFA/Freddie g-fee data), re-run the full-book reweight, composed-basis cell, and tab:wal row (run-class; the composed 100.0% cell will move — landing rule pre-commits its disposition; the marginal is basis-invariant and must not move, which is itself the parity check). If G1 shows the parse already carries note rates, the finding collapses to a labeling fix — either way the manuscript gains the convention labels.

## WP-H — Benchmark integrity (R2; S3/S6/S11; DA unexamined premise; S7)

- **H1. Zero-months diagnosis** (internal): trace the four exact-zero months in the SOMA back-out (calendar alignment vs factor-date artifact); repair by pair-conserving redistribution as App. N already prototypes; re-score the timing exhibits on the repaired series OR promote the honest conclusion ("this monthly benchmark cannot adjudicate timing") from App. N to §III.B/§VI.B. Landing rule pre-commits which, per outcome.
- **H2. 5.2311% check** (internal): verify the rate series and month alignment in `sign_forcing_stats` against MORTGAGE30US. If a one-month offset exists, fix and re-run the (stronger) sign-forcing stats; g_{i,t} alignment implications swept in the same spec.
- **H3. Expectations conditioning** (wording + external lookup): state the May-2022 OMO projection's conditioning rate path; restate the 49% / 23–49% / 38–80% ratios as upper bounds on a mixed (lock-in + rate-path) surprise or drop them. ⚖ if dropped (they are quoted in §III.B and the conclusion).
- **H4. Aggregate-policy referent** (wording): one paragraph answering the DA — whether the MBS shortfall constrained any stated FOMC objective given Treasury runoff at its own cap; frames "institutional cash-flow cost" as composition accounting. No run needed; H.4.1 facts only.

## WP-I — Elasticity discipline (DA-M4; R1-W5; S4/S5)

- **I1. Within-stratum episode confrontation** (run-class, existing cells): the S-curve exhibit by incentive bucket, age-matched, over vintage×coupon×FICO×LTV cells. This is the exhibit all three technical reviewers independently asked for. Landing rule: pre-commit the disposition under (a) gradient gap persists within strata (the composition-confounding defense falls; the assembly gains an upward entry — symmetric with C2's logic), (b) gap closes (the defense is vindicated and stated as tested).
- **I2. Transport assumptions named** (wording): ZIP→loan ecological transport and survival selection (34,734/75,000 pre-window prepaid) join the three named caveats at tex 253-255.
- **I3. Attenuation sensitivity** (run-class, cheap): β₁ scaled by a survival-selection attenuation factor over a stated range as a tab:uncertainty column — or a gamma-frailty variant if you want the structural version (costlier; the scaling sensitivity answers R1's question).

## WP-J — Cannot-fix / declined register (state these in the response letter, do not attempt)

1. **Loan-purpose field for bounding s** (R2's suggestion): NOT executable — the raw Freddie/Fannie source is gone (TECHNICAL/data-ceiling record; only the frozen ingested columns exist, purpose was never ingested). The feasible substitute is B3-option-(iii)'s argument bound (rate/term refi shut at −380bp) — an argument, not an estimate, and the letter should say exactly that.
2. **Housing-activity term in h₀** (R2-W2's primary ask): a new headline-estimator specification — posture-heavy, re-opens the frozen production spec, and the clean covariate (months' supply / EHS) enters the model the ABM friction index was deliberately kept out of Path B for. Feasible alternative that answers the substance: a **scaled-null variant** (baseline scaled by the realized non-rate share of the turnover decline, Aladangady-anchored) reported as an additional assembly member — run-class on existing machinery, signs the direction R2 identifies, and enters the symmetric assembly as its upward-bias entry. ⚖ Recommend the variant, not the term.
3. **Outcome holdout**: K1 kill stands (circular).
4. **Vintage marginal overlay** (S8): feasible as a bound (out-of-window cohort cells exist from the Fannie ingest — the same cells that produced the $11.7B level bound can score both legs Ginnie-overlay-style); spec it as run-class. The 2022-vintage characterization fix (tex 263) is wording and unconditional.
5. **GE counterfactual / Fonseca-Liu-Mabille-style model**: out of scope for this paper; cite and position (P3 refs) instead.

## Sequencing and decision points

**Order:** WP-A7 hygiene + A1/A3/A4/A5 (wording, immediate) → your ⚖ decisions (A2 rename, A6 abstract, B3 posture, C2 presentation, D headline disposition, E structure/split, H3 ratios) → specs written for B2, C1–C6, D, F1, G2, H1–H2, I1, I3, J2, J4 (each with landing rules, batched for your sign-off) → runs → B1 assembly rebuild + posture landing (one commit, gates/tests/letter) → WP-E structural moves last (they renumber everything).

**Your decision points, collected:** (1) floor rename now vs at venue revision; (2) abstract policy-sentence form caveat; (3) posture: retire / restate / defend-s≈1; (4) convolved vs partial-identification presentation; (5) mixture-curve headline disposition; (6) results-section rework scope and the Danish split (re-raised by the panel; standing decision is yours); (7) drop vs restate the expectations ratios; (8) scaled-null variant in lieu of a housing-activity term; (9) OK for the Danish external data pull.

**What this plan does not do:** change any committed number outside a pre-committed landing rule; re-litigate the length decision beyond the one required flag; touch anything the panel did not raise.

---

## Decision record (Eugene, 2026-07-28: "I think we can do all of them" + agent/verify architecture)

All eight decision points taken per coordinator recommendation, vetoable individually: (1) posture RETIRED — range + symmetric assembly carry; (2) abstract form caveat YES; (3) floor rename INTERIM (definitional sentence now, global rename at venue revision); (4) convolution as labeled second line; (5) mixture curve RUNS, headline anchor unchanged this round, interior value named in form-conditionality statements; (6) in-paper results rework YES, Danish SPLIT DEFERRED to venue decision; (7) expectations ratios RESTATED as upper bounds; (8) scaled-null variant YES. Execution architecture per Eugene: opus agents draft each subsection; Fable verifies every span/count/claim and applies; ALL runs executed by the coordinator in-session under committed specs (agents never near a run). Recon snapshot at execution start: gates ALL PASS, 431 tests/3.2s, binding ×7 + and-form ×2, percentile ×4, hull ×7, 11.06 ×5, positive-at-every ×6, 9.17 ×7, letter claim 226.

---

## Round-28 execution log (running; coordinator-verified at each step)

- **WP-A EXECUTED in three commits, all gates/tests green after each:** `8b3d04f` A-core (C1 universals ×4+1 sibling, C3 interval referent at 4 sites + pins→bounds + gate #99 `interval_referent` span, M3 Na/independence ×3; abstract 226→235); `ff80ed7` A-frames (§IV falsification frame retired w/ scoped verdicts kept, contributions 5→3 w/ nyfed2022 arbitration, trilemma Variant B + tex-609 companion, abstract policy-sentence form caveat; 235→248; letter discloses); `0db917d` A-hygiene (13 items + uniform Path-A label ×5 sites + tex-432 sign fix; liebersohn2024 premise-fails SKIPPED — entry is correct, answer in letter). Abstract now 248 words, letter recount current, wrong-count list gained 226/235. 434 tests.
- **Specs committed:** `a4cffef` (B2/C4/C5 + C1/C2/C3/C6), B2 script `5488467`, D/F1/I1/I3/J2/S8 archive with the B2 artifact commit. Load-bearing spec findings: C4 needs a pre-committed script patch or it clobbers gate #84's artifact; PSA_SPEED is a def-time default (runtime mutation is a silent no-op — patch `lh.baseline_hazard`); `bootstrap_se.py` as `__main__` would overwrite the committed v3 draws; [+9.17,+9.23] has SIX literal sites + tex 65/343 claim-only (tex 1360 is a different object); I1's literal within-stratum gradient is unidentified (coupon ∈ stratum id) → composition-standardization design w/ feasibility gate; J2's corrected member lies BELOW the headline (the "upward" is the bias direction, not the member); D's s-orientation: s = strictly-involuntary share, "well under half" → named point s≈0.4 (R1's share = 1−s — symbol decision is Eugene's); S8 + I1-G3 need main-checkout inputs (fannie_quarters/, cohort_month_panel_fannie.parquet, floor_form_offwindow/ parquets — gitignored, absent in worktree); S8 rider: tex 263's "near or above prevailing market rates" is refuted by composition_shift's own joint cells (72.2% of the 2022 vintage below 3.0% pass-through).
- **B2 EXECUTED — Branch A.** s=0.5 → +3.36pp/+$25.71B; s=0.25 → +1.82pp/+$13.93B; parity P1–P4 bit-exact vs oos anchors; ratios super-proportional as signed ex ante (0.603/0.327 vs in-sample 0.535/0.273). No range-conditionality statement forced. Rows queue for the WP-B1 symmetric assembly rebuild alongside sub-band +3.47, concave-alone +5.06, additive +11.2, Fonseca +11.5.
- **NEXT (in order):** B1 assembly rebuild + posture retirement (gate #98 spans; tex 312/255 collisions mapped in specs); C3 PSA sweep (cheapest, decides layer-ranking sentence); C5; C4 (script patch first); C1→C2; D; F1 (band mandatory per RULE-A′); I3; J2; C6; I1+S8 (main checkout); then WP-F3/G/H wording+checks; WP-E structural LAST (renumbers everything). All ⚖ landings queue for Eugene per spec branch text.
- **B1 EXECUTED** (`e244a46`): seventh qualification lines both directions up (downward: concave-alone +5.06, sub-band +3.47, moving-share +3.36/+1.82; upward: additive +11.2, Fonseca +11.5); "middle member" posture RETIRED at all 3 sites (gate #98 span re-pinned `posture_retired_range_carries`); tab:assembly +4 rows, tab:verdicts +2 rows (zero new source lines — no renumbering); B2 landed at tex 255; letter updated. DA-C2 fully discharged.
- **C3 EXECUTED, PSA_WIDER** (`258536b` spec, artifact follows): sweep widths 13.14pp in-sample / 14.77pp off-window vs floor-read 5.93pp. Branch (a) tex landing (layer-ranking sentence at tex 312 = gate #98 `posture_binding_layer` span + tab:uncertainty rows + tex 343 lead-in) is ⚖ QUEUED FOR EUGENE with the spec's pre-drafted replacement. Off-window expectation-band misses (PSA75 +0.86 vs 1.5-2.5; PSA125 +11.4 vs 8.5-10; PSA150 +15.6 vs 11-14) reported; in-sample all inside.
- **⚖ QUEUE FOR EUGENE (accumulating):** C3 branch-(a) layer-ranking restatement; F1 band variant (V1 0-1% vs V2 0-3%, rec V2); D mixture-curve symbol (s vs 1-s) + named point (rec s=0.4); S8 tex-263 rewrite; J2 assembly-entry wording; I1 branch wording when run; C4 branch-(a) hierarchy strikes when run.
- **C5 EXECUTED, branch (i) — EXPECTATION MISS THAT MATTERS** (`881f41c` spec, artifact + serialization fix follow): the covariate LEVEL swing does NOT compress at the headline floor — it EXPANDS, 7.02pp in-sample → 8.24pp off-window (factor 1.174 vs the pre-committed [0.45,0.60] band), and EXCEEDS the off-window marginal (ratio 1.48). The MARGINAL swing collapses +1.93 → +0.10pp (nearly covariate-invariant where the headline lives). All parity gates incl. the G7 cross-artifact replay bit-exact. Landing branch (i) is ⚖: the tex-301 "largest single sensitivity" sentence must be STRENGTHENED at the headline floor (level swing exceeds the marginal) while adding the counter-fact (the marginal itself barely moves there). QUEUED for Eugene with both facts.
- **C4 LAUNCHED in background** (script patch `a3ab3cf`: floor-tagged outputs, gate-#84 overwrite guard, floor-conditional anchors, tier neutered): 200 reps at the in-sample 4.0% floor; G3 pre-hashes recorded at hazard/data/_c4_g3_prehash.txt; landing branches (a)/(b) per spec on width vs 0.5pp.
- **D LAUNCHED in background** (spec+script committed): mixture curve, 48 runs, endpoints via production code paths, named point s=0.4 + s=0.25; landing PASS/STOP-1/2/3 per spec; symbol choice ⚖ Eugene.
- **2026-07-29, EUGENE AUTHORIZED THE FULL QUEUE.** Landings executed (`ff5cf59`): C3 branch-(a) layer ranking scoped (gate #98 `posture_binding_layer` re-pinned: "widest layer with a coverage property"); C5 branch-(i) covariate sentence strengthened w/ counter-fact at tex 301/263/911 + three tab:uncertainty cells; S8 tex-263 characterization corrected (72.2%-below-3.0% verified by the coordinator from composition_shift cells); runindex +3 / verdicts +2 rows. One banned-literal collision (93.4\%, gate `no_stale_934`) caught by the gate suite and reworded to standalone units — the gate architecture catching the coordinator's own edit.
- **D first run STOPPED honestly, spec AMENDED with labels, re-running:** continuity probe 1e-15 was float-noise-tight (realized 3.5e-15; identity probes at 1e-16); the curve genuinely plateaus (~0.03pp decline past s≈0.7 — pool-depletion, engine parity bit-exact). Amendments A1 (1e-13) + A2 (monotone-to-plateau, 0.1pp band) labeled POST-RUN in the header and will carry to the verdicts row. SUBSTANCE PREVIEW from run 1: named s=0.4 off-window ≈ +10.7pp, s=0.25 ≈ +9.7 — at the paper's own "well under half" semantics the mixture sits near the ADDITIVE end; +5.6 is the conservative ENDPOINT of the form dimension. Landing text will say exactly that.
- **F1 LAUNCHED** (spec+script committed): 13-cell fine grid + artifact-only realized-anchor cell (22.4%/yr w/ four confounds); band V2 (0–3%) authorized; landing after gates.
- **In flight:** C4 bootstrap (bg), D re-run (bg), F1 (bg); drafting agents on C1 (v2 inference script — C2 blocker), I3+J2, C6 trio. Landings queued on completion: D named-point clauses at the four hull sites; F1 band at tab:danish/tex 464/fig:gapsweep + near-equality retirement; C4 branch (a)/(b) at the 8 disposition sites.
- **2026-07-29 second half — the run program's core is now EXECUTED AND LANDED.** F1 (`e0c6ca2`): band V2 landed everywhere incl. edge-labeling at intro/conclusion, near-equality retired to an edge property, realized-anchor cell +$1,212B artifact-only. H2 CLOSED as no-error (argmin month = Aug 2022, now named in tex) + WP-F3 landed (`fcc56bb`): tax-law correction, TBA series-vs-pool rewrite w/ berg2018, "Danish payoff rule" restriction, transplant-scope enumeration w/ signed omissions. I2+H3+H4 (`2cbcee5`). D landed (`d1146fb`): mixture curve is the form exhibit — KEY SUBSTANCE: the curve rises to +9.7 by s=0.25 / +10.7 at s=0.4 and plateaus ~+11.2, so the paper's own "well under half" semantics point at the hull's UPPER region and +5.6 is the conservative s=0 ENDPOINT; the circular aggregate-fit selection rule is REPLACED. C1 executed + landed (`1e59852`): CONFIRM_PRINT_MOVE — **the binding interval is now printed [+2.9, +8.7]** (Webb primary [+2.855,+8.678]; expectation MISS: slight narrowing, reported; G*=5.9, df=5.1; CR2/CR3@df widen to [+2.4,+9.2]/[+2.3,+9.6], WCR [+2.3,+9.1] — full ladder + B/DGP/studentization now in-tex); 12 occurrences + gates #98/#99 + ordering assert + count gate + letter + 2 test fixtures moved in one commit. C4 executed + landed branch (a) (`6a804ef`): in-sample cluster interval [+8.27,+10.19] (30.8x vs the retracted scheme); all 8 disposition sites; "negligible at reporting precision" struck; box (11.1) still governs vs cluster (1.9) — conclusion survives on corrected premise. I3 executed + landed (`a4ce0be`): attenuation +5.1/+4.6/+3.7 at theta=.25/.5/1, all cells inside pre-committed bands, kept OUT of the assembly. **NEW ZERO-SLACK STATE: binding literal is `$+2.9$ to $+8.7$` x8 (+1 self-reference in the ladder), and-form `$+2.9$ and $+8.7$` x2, bracket `$[+2.9, +8.7]$` x3; the OLD pair survives x1 as the labeled Rademacher secondary at tex 592.**
- **REMAINING:** J2 running (root ~phi 0.75-0.78; landing = assembly row + tex 98 forward ref + specbox disclosure, wording "corrected member lies BELOW"); C6 trio (agent drafting); C2 (unblocked by C1's t* CSVs; P4 interaction probe optional w/ mandatory caveat); I1+S8 runs (MAIN CHECKOUT inputs); WP-G1 coupon check; H1 zero-months diagnosis; WP-E structural rework LAST; final letter pass + build check (tectonic) at round close.
